{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "sourceMap": true, // 개발자도구에서 Source탭 내 코드목록에 ts파일도 추가하여 디버깅에 용이하도록
    // "lib": ["dom"]
    // "exclude": ["excludts.ts"], // compile 되지않도록 설정 (보통은 node_modules를 넣지만 기본적으로 node_modules는 제외시킨다. 하지만 exlude를 설정하면 node_modules꼭 넣어주자)
    //
    "outDir": "", // 컴파일 후 생성되는 js파일이 생성될 폴더명
    "rootDir": "", // 시작하는 루트 폴더
    "removeComments": true, // 모든 주석이 컴파일된 js파일에서 제거됨(파일 사이즈 줄이기)
    "noEmitOnError": false, // 에러가있는경우 js로 컴파일 하지않는다.
    "noImplicitAny": true, // 암묵적으로 any타입을 가지는것을 허용하지 않는다. (false로 세팅하는경우는 js->ts로 마이그레이션하는경우) true를 유지하도록 하자.
    "strictNullChecks": false, // null, undefined 등이 할당되지 않도록 한다.

    /* 사용하지않는 코드들 정리 */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": false,
  }
}
